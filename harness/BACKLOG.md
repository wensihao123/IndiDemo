---
updated: 2026-06-18
---


# BACKLOG (Producer)

> 范围的单一事实来源。每个想法只落进一个桶,并附理由,避免下周重新吵。
> 默认偏向 LESS:中途冒出的新想法,默认答案是 "Later"。

## v1 scope line(可上架/可验收的 v1 = 这一句)
一个能跑通**完整闭环**的垂直切片:挂机探索自动打怪、掉装备/材料 → 回城换装+最简强化变强
→ 打过更硬的怪。含悬浮窗外壳、4 格队伍(v1 只填 1 个战士)、**后台持续推进**、最简存档。

## Done(已完成 —— v1 切片累积)
- ✅ **01-floating-window-shell**(2026-06-18)— 底部全宽常驻悬浮窗、800×250 居中主区、收起/恢复、
  待机微动占位角色、F1 收起 / F2 置顶、per-pixel 透明收起 handle。设计→实现→接线→审查→清理 全链路
  走通:`godot --headless --check-only` 退出码 0、人工 Play + EI Phase 2 验收 OK、REVIEW must-fix/nit 全清。
- ✅ **02-auto-combat-loop**(2026-06-18)— 1 战士自动打 2 关 × (3 普通场景 + Boss)、固定步长 tick(后台持续推进)、
  4 格队伍数据地基(v1 填 1 战士)、掉落事件流(白/蓝/金分级 FX)、Boss 永久解锁 + 团灭回退刷怪 + 推进/修整。
  **全链路收口:** 实现(32 gdUnit4 用例全绿 / `--import` 0)→ 审查(REVIEW APPROVE,0 must-fix,含 F1 美术接线增量)
  → 设计 S2(回血/难度模型拍板)→ 美术规格+验收(13 件正式素材整组 PASS)→ 接线(EI Part1+2 accepted)→ 人工 Play 验收 OK。
  携出两条 v2 想法已停车(等级/经验、车轮战→团战,见 Later)。

## Now(committed,当前在做 —— 一次只做一件)
1. **03-loot-equipment** — 暗黑式掉落:2-3 槽位、2-3 稀有度(白/蓝/金)、1-2 词条
   (攻击/生命);背包与穿戴数据。**承接 02 的 `loot_dropped(kind, rarity)` 事件边界**
   (02 只产"掉了 X 稀有度一件"事件,物品实例/词条 roll 归 03)。
   - 不含:套装、宝石、复杂词条池、词条重铸。

## Next(排队中,按优先级)
2. **04-town-gear-upgrade** — 城镇最简版:换装 + 1 个最简打造/强化(材料 +1 武器)。
   - 不含:招募、技能升级、其它打造线。
3. **05-difficulty-progression** — 闭环收口:变强后能打过一个更硬的怪 / 第二波。
   - 不含:难度曲线调优、关卡设计、多区域。
4. **06-save-load** — 最简 save/load(队伍、背包、进度)。挂机游戏刚需。
   - 不含:云存档、多档位、自动存档策略调优。

## Later / v2(明确推后,已停车未杀)
- 招募系统 / 坦克·法师·射手·牧师等其它职业 / 4 人实际组队 build 深度
- **等级 / 经验系统** —— 击杀给经验、升级长 HP/攻击。源:02 playtest(2026-06-18)。
  扩展性 = **低成本纯增量**:`PartyMember`/`EnemyDef` 加字段、在 `tick_combat` 击杀分支挂
  `_award_xp`,战斗循环不动;升级曲线走 Resource(守 hard-NO)。**跨局保留等级依赖 06 存档**
  (现队伍每局由 director `@export` 重建)。需 Game Designer 定经验来源 / 升级曲线 / 升哪些属性。
- **车轮战 → 团战(单场多敌同屏)** —— 现为一只只打的车轮战;改成一波多敌同时打。源:02 playtest(2026-06-18)。
  扩展性 = **中等,一处干净重构**:队伍侧已是数组(`_party_total_attack`/`_front_living_member`,
  即 D2 抽象 DPS 交换的预留),敌方侧是单实例(`_enemy_def`+`_enemy_hp`)→ 需引入运行时敌人实例
  (照抄 `PartyMember`)+ 数组;`SceneConfig.enemy`→敌人数组;解算加**目标选择规则**(集火/分散/AoE,
  Game Designer 定);"清场 / 一轮"按整波重定义。掉落 / 团灭回退 / 解锁 / 倒计时**不用动**(只认事件)。
- 技能树与技能升级
- 多探索区域 / Boss
- 离线结算(完全关闭程序后的收益)
- 套装、宝石、复杂词条池、词条重铸
- **02 横版战斗演出打磨** —— 怪精灵、攻击/受击动画、伤害飘字、掉落迸出等全套表现。
  符号/轻演出版验证了循环乐趣后再上;属打磨而非核心验证,正式美术阶段或单独立项再做。(源:02 IDEA §8)
- 透明窗 / 点击穿透 / 多显示器精细适配
- **全局热键(窗口失焦时也响应 F1 收起 / F2 置顶)** —— Godot 无内建全局热键,需 OS 级注册或插件
  (撞 hard-NO 不引插件);本期主路径"点 handle / 收起按钮"在窗口聚焦时必可用,已够用。(源:01 R2)
- Linux / Mac 平台

## Cut(决定不做,附理由)
- (暂无)—— 目前没有被否决的想法;新想法默认进 Later 而非直接 Cut。

## Decision log
- 2026-06-17 — v1 定为"单战士垂直切片",招募/多职业/组队深度推到 v2。
  Why: 完整愿景对独游过大;先用 1 个战士跑通完整闭环验证核心乐趣(支柱 1+2)。队伍数据
  按 4 格设计,保证日后加人不必重写地基。
- 2026-06-17 — Now 第一项 = 悬浮窗外壳(01),先于战斗循环。
  Why: 悬浮窗是项目最大技术未知数,也是支柱 1"它首先是个伙伴"的物理载体;地基不验证,
  上层做得再好也可能卡在"窗口跑不起来"。
- 2026-06-17 — "后台持续推进"列为 MVP 必须(在 02 内);"离线结算"推到 v2。
  Why: 后台推进是"挂机"本质、陪伴支柱的根基;离线结算只是其增强,可后补,不阻塞 v1。
- 2026-06-18 — **01-floating-window-shell 验收完成,移入 Done;Now 推进到 02-auto-combat-loop。**
  Why: 01 全链路(设计→实现→接线→审查→清理)通过,headless 校验退出码 0 + 人工/EI 验收 OK;
  项目最大技术未知数(桌面悬浮窗)已证明可落地,可安心推进战斗循环。一次只做一件,02 进 Now。
- 2026-06-18 — "全局热键(失焦也响应)" → Later/v2。
  Why: 不阻塞 v1——本期点 handle/按钮(聚焦时必可用)已满足收起/置顶;真全局热键需插件,撞
  hard-NO。停车未杀,正式做桌面体验打磨时再议。(源:01 EI/Reviewer 建议另立项)
- 2026-06-18 — 非范围备注:INTEGRATION-STEPS 内 Godot 可执行文件名 `Godot_v4.6.3-stable_win64.exe`
  与本机实际 `godot.exe` 不符 → 属文档修正(非 scope 项),已记在 01 HANDOFF,留下次 EI session 顺手改。
- 2026-06-18 — **02 演出粒度 = 符号/轻演出版(simplify);横版全套演出 → Later。**
  Why: 02 的核心验证是"自动战斗循环像不像好伙伴"+ 4 格队伍数据 + 后台持续推进,**不是横版美术**;
  全套演出是一大坨美术/动画成本却不回答核心问题。01 已有微动占位战士,符号版足够"瞥一眼看懂、
  感到伙伴卖命"。先用最小美术验证乐趣,演出留打磨。(源:02 IDEA §8 Producer flag,用户拍板)
- 2026-06-18 — **02 playtest 冒出两条新想法 → Later/v2(默认偏 LESS,不塞进 02)。**
  ① 等级/经验系统、② 车轮战→团战(单场多敌)。Why: 都不是 02 核心验证("循环+4格+后台推进")的必要件,
  塞进来会拖长本期闭环。已评估两者基于现 code base 的扩展成本(等级=低/纯增量、团战=中/一处干净重构),
  地基无挡路硬伤,可安心推后。落地前各需 Game Designer 定规则(经验曲线 / 团战目标选择)。(源:用户 playtest)
- 2026-06-18 — **02-auto-combat-loop 验收完成,移入 Done;Now 推进到 03-loot-equipment。**
  Why: 02 全链路(设计→实现→审查 APPROVE→美术规格/验收→接线→人工 Play)通过,32 用例全绿 + `--import` 0;
  自动战斗循环 + 4 格队伍地基 + 后台持续推进三块核心已验证,符号/轻演出版足够看懂"伙伴卖命"。一次只做一件,
  03 进 Now——它正好接 02 已留好的 `loot_dropped(kind, rarity)` 事件边界(02 只产事件,物品/词条归 03),衔接干净。
