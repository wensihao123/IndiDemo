---
updated: 2026-06-18
---

# BACKLOG (Producer)

> 范围的单一事实来源。每个想法只落进一个桶,并附理由,避免下周重新吵。
> 默认偏向 LESS:中途冒出的新想法,默认答案是 "Later"。

## v1 scope line(可上架/可验收的 v1 = 这一句)
一个能跑通**完整闭环**的垂直切片:挂机探索自动打怪、掉装备/材料 → 回城换装+最简强化变强
→ 打过更硬的怪。含悬浮窗外壳、4 格队伍(v1 只填 1 个战士)、**后台持续推进**、最简存档。

## Done(已完成 —— v1 切片累积)
- ✅ **01-floating-window-shell**(2026-06-18)— 底部全宽常驻悬浮窗、800×250 居中主区、收起/恢复、
  待机微动占位角色、F1 收起 / F2 置顶、per-pixel 透明收起 handle。设计→实现→接线→审查→清理 全链路
  走通:`godot --headless --check-only` 退出码 0、人工 Play + EI Phase 2 验收 OK、REVIEW must-fix/nit 全清。

## Now(committed,当前在做 —— 一次只做一件)
1. **02-auto-combat-loop** — 1 战士在 1 区域自动打 2-3 种怪、自动产出掉落事件。
   **含两条 MVP 核心系统(需在本期敲定地基):**
   ① 队伍按 **4 格 slot** 设计,v1 只填 1 个战士(数据支持 N 人,避免日后重写);
   ② **后台持续推进**——窗口在后台/玩家在用别的软件时,探索照常推进。
   - 不含:其它职业、技能树、Boss、多区域、离线结算。

## Next(排队中,按优先级)
2. **03-loot-equipment** — 暗黑式掉落:2-3 槽位、2-3 稀有度(白/蓝/金)、1-2 词条
   (攻击/生命);背包与穿戴数据。
   - 不含:套装、宝石、复杂词条池、词条重铸。
3. **04-town-gear-upgrade** — 城镇最简版:换装 + 1 个最简打造/强化(材料 +1 武器)。
   - 不含:招募、技能升级、其它打造线。
4. **05-difficulty-progression** — 闭环收口:变强后能打过一个更硬的怪 / 第二波。
   - 不含:难度曲线调优、关卡设计、多区域。
5. **06-save-load** — 最简 save/load(队伍、背包、进度)。挂机游戏刚需。
   - 不含:云存档、多档位、自动存档策略调优。

## Later / v2(明确推后,已停车未杀)
- 招募系统 / 坦克·法师·射手·牧师等其它职业 / 4 人实际组队 build 深度
- 技能树与技能升级
- 多探索区域 / Boss
- 离线结算(完全关闭程序后的收益)
- 套装、宝石、复杂词条池、词条重铸
- 透明窗 / 点击穿透 / 多显示器精细适配
- **全局热键(窗口失焦时也响应 F1 收起 / F2 置顶)** —— Godot 无内建全局热键,需 OS 级注册或插件
  (撞 hard-NO 不引插件);本期主路径"点 handle / 收起按钮"在窗口聚焦时必可用,已够用。(源:01 R2)
- Linux / Mac 平台

## Cut(决定不做,附理由)
- (暂无)—— 目前没有被否决的想法;新想法默认进 Later 而非直接 Cut。

## Decision log
- 2026-06-17 — v1 定为"单战士垂直切片",招募/多职业/组队深度推到 v2。
  Why: 完整愿景对独游过大;先用 1 个战士跑通完整闭环验证核心乐趣(支柱 1+2)。队伍数据
  按 4 格设计,保证日后加人不必重写地基。
- 2026-06-17 — Now 第一项 = 悬浮窗外壳(01),先于战斗循环。
  Why: 悬浮窗是项目最大技术未知数,也是支柱 1"它首先是个伙伴"的物理载体;地基不验证,
  上层做得再好也可能卡在"窗口跑不起来"。
- 2026-06-17 — "后台持续推进"列为 MVP 必须(在 02 内);"离线结算"推到 v2。
  Why: 后台推进是"挂机"本质、陪伴支柱的根基;离线结算只是其增强,可后补,不阻塞 v1。
- 2026-06-18 — **01-floating-window-shell 验收完成,移入 Done;Now 推进到 02-auto-combat-loop。**
  Why: 01 全链路(设计→实现→接线→审查→清理)通过,headless 校验退出码 0 + 人工/EI 验收 OK;
  项目最大技术未知数(桌面悬浮窗)已证明可落地,可安心推进战斗循环。一次只做一件,02 进 Now。
- 2026-06-18 — "全局热键(失焦也响应)" → Later/v2。
  Why: 不阻塞 v1——本期点 handle/按钮(聚焦时必可用)已满足收起/置顶;真全局热键需插件,撞
  hard-NO。停车未杀,正式做桌面体验打磨时再议。(源:01 EI/Reviewer 建议另立项)
- 2026-06-18 — 非范围备注:INTEGRATION-STEPS 内 Godot 可执行文件名 `Godot_v4.6.3-stable_win64.exe`
  与本机实际 `godot.exe` 不符 → 属文档修正(非 scope 项),已记在 01 HANDOFF,留下次 EI session 顺手改。
